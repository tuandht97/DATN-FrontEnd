export interface Stock {
    id: string
    code: string,
    amount: number,
    price: number,
    owner: string,
    address: string,
    squareMeter: number,
    description: string,
    createdDate: Date
}